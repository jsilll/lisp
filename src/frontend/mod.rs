/// The `token` module contains the possible tokens in the language.
pub mod token;

/// The `lexer` module contains the code for generating tokens from the source 
pub mod lexer;

/// The `parser` module contains the code for generating an AST from the source code.
pub mod parser;